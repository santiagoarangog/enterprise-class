export default interface InfoPagesI18nContract {
  expiredLink: {
    expiredLinkTitle: string
    expiredLinkMessage: string
    expiredLinkButton: string
    notification: {
      title: string
      message: string
    }
  }
}
